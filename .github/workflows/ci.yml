name: CI

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  GO_VERSION: "1.23.4"

jobs:
  lint-test:
    name: Lint, Build and Test
    runs-on: ubuntu-latest
    if: github.event.pull_request.draft == false || github.event_name != 'pull_request'
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
          cache-dependency-path: go.sum

      - name: Cache Go build
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-go-build-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-build-

      - name: Check for dependency updates
        if: github.event_name == 'pull_request'
        id: deps
        run: |
          echo "Checking for direct dependency updates..."
          
          # Debug: ¿Qué versiones ve Go?
          echo "DEBUG: Current environment:"
          go version
          
          # 1. Obtener la lista de módulos directos (sin // indirect)
          DIRECT_MODS=$(go list -m -f '{{if not .Indirect}}{{.Path}}{{end}}' all)
          
          # 2. Ejecutar list -m -u y procesar manualmente
          # Forzamos GOPROXY para asegurar que consulte la red
          echo "DEBUG: Fetching updates from proxy..."
          RAW_UPDATES=$(GOPROXY=https://proxy.golang.org go list -m -u all 2>/dev/null | grep "\[" || true)
          
          echo "--- RAW UPDATES FOUND ---"
          echo "$RAW_UPDATES"
          echo "--- END RAW UPDATES ---"
          
          FINAL_LIST=""
          while read -r line; do
            [ -z "$line" ] && continue
            
            # Formato de línea: path version [latest_version]
            mod_path=$(echo "$line" | awk '{print $1}')
            
            # Solo procesar si es una dependencia directa
            if echo "$DIRECT_MODS" | grep -qx "$mod_path"; then
              current=$(echo "$line" | awk '{print $2}')
              latest=$(echo "$line" | awk '{print $3}' | tr -d '[]')
              
              if [ -n "$latest" ] && [ "$current" != "$latest" ]; then
                FINAL_LIST="${FINAL_LIST}${mod_path}: ${current} → ${latest}\n"
              fi
            fi
          done <<< "$RAW_UPDATES"

          if [ -n "$FINAL_LIST" ]; then
            echo "has_updates=true" >> $GITHUB_OUTPUT
            {
              echo "updates<<EOF"
              echo -e "$FINAL_LIST"
              echo "EOF"
            } >> $GITHUB_OUTPUT
            echo "Found updates!"
          else
            echo "has_updates=false" >> $GITHUB_OUTPUT
            echo "No updates found for direct dependencies."
          fi

      - name: Check for Go version update
        if: github.event_name == 'pull_request'
        id: go_version
        run: |
          echo "Checking for Go version updates..."
          CURRENT_GO="${{ env.GO_VERSION }}"
          LATEST_GO=$(curl -s 'https://go.dev/dl/?mode=json' | jq -r '.[0].version' | sed 's/go//')
          echo "Current Go version: $CURRENT_GO"
          echo "Latest Go version: $LATEST_GO"
          if [ "$CURRENT_GO" != "$LATEST_GO" ]; then
            echo "has_update=true" >> $GITHUB_OUTPUT
            echo "current=$CURRENT_GO" >> $GITHUB_OUTPUT
            echo "latest=$LATEST_GO" >> $GITHUB_OUTPUT
          else
            echo "has_update=false" >> $GITHUB_OUTPUT
          fi

      - name: Comment on PR with dependency updates
        if: github.event_name == 'pull_request' && (steps.deps.outputs.has_updates == 'true' || steps.go_version.outputs.has_update == 'true')
        uses: actions/github-script@v7
        with:
          script: |
            const hasDepUpdates = '${{ steps.deps.outputs.has_updates }}' === 'true';
            const hasGoUpdate = '${{ steps.go_version.outputs.has_update }}' === 'true';
            
            let body = '## 📦 Dependency Update Check\n\n';
            
            if (hasGoUpdate) {
              body += '### 🐹 Go Version\n';
              body += '| Current | Latest |\n';
              body += '|---------|--------|\n';
              body += `| \`${{ steps.go_version.outputs.current }}\` | \`${{ steps.go_version.outputs.latest }}\` |\n\n`;
            }
            
            if (hasDepUpdates) {
              body += '### 📚 Direct Dependencies\n';
              body += '```\n';
              body += `${{ steps.deps.outputs.updates }}`;
              body += '\n```\n\n';
            }
            
            body += '> ℹ️ This is an informational comment. Updates are not required for this PR to pass.\n';
            body += '> Run `go get -u <module>` to update a specific dependency.';
            
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number
            });
            
            const botComment = comments.find(c => 
              c.user.type === 'Bot' && 
              c.body.includes('📦 Dependency Update Check')
            );
            
            if (botComment) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: botComment.id,
                body: body
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: body
              });
            }

      - name: Verify go mod tidy
        run: |
          go mod tidy
          git diff --exit-code

      - name: Security Check (govulncheck)
        run: |
          go install golang.org/x/vuln/cmd/govulncheck@latest
          govulncheck ./...

      - name: Security Audit (gosec)
        uses: securego/gosec@master
        with:
          args: ./...

      - name: Check formatting (gofmt)
        run: |
          fmt_out=$(gofmt -l .)
          if [ -n "$fmt_out" ]; then
            echo "Go files need formatting:"
            echo "$fmt_out"
            exit 1
          fi

      - name: Build
        run: go build -v ./...

      - name: Lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: v2.7.2

      - name: Test (race + coverage)
        run: go test -v ./... -race -coverprofile=coverage.txt -covermode=atomic

      - name: Upload coverage reports to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
