{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug StockTracker",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/tracker/main.go",
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}
